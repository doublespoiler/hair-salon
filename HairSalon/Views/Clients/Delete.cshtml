@{
  Layout = "_Layout";
}

@model HairSalon.Models.Client

<h1>Are you sure you want to delete this client?</h1>

@Html.DisplayNameFor(model => model.ClientId) : @Html.DisplayFor(model => model.ClientId) : @Html.DisplayNameFor(model => model.ClientName): @Html.DisplayFor(model => model.ClientName)

@using (Html.BeginForm())
{
  <input type="submit" value="Delete" class="btn btn-lg" />
}
@Html.ActionLink("Cancel", "Show", new {id = Model.ClientId}, new{@class="btn btn-lg"})